# Adepa Pork Hub

> **Fresh. Spiced. Ready for Every Meal.**

The legacy Laravel/Vite stack that ran on Hostinger has been retired — it is
no longer used anywhere in this project.

The current application (Next.js + Neon Postgres, deployed on Vercel) lives
on the **`next-rebuild`** branch, in `frontend-next/`. This branch (`main`)
has not yet been updated to match; it will be replaced once `next-rebuild`
is promoted.
